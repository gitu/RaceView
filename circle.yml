dependencies:
  pre:
    - npm install --global optipng-bin firebase-tools
    - gem install compass
test:
  pre:
    - bower install
  override:
    - grunt test
  post:
    - grunt build

deployment:
  master:
    branch: master
    commands:
      - firebase -s deploy



